# Git Remote Handling:

## Check Existing Remotes:
  `git remote -v`

## Add a New Remote:
  `git remote add origin git@localhost:/home/git/ktags.git`
  `git remote add origin git@github.com:kribakarans/ktags.git`

## Update Remote URLs (Optional):
  `git remote set-url origin https://new-url.git`

## Remove Git Remote
  `git remote remove origin`
